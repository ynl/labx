# AI Creativity Showcase App

一个使用 Expo React Native 和 Supabase 构建的 AI 创意作品展示应用。用户可以浏览、上传和分享各种 AI 生成的艺术作品。

## ✨ 功能特性

- 📱 **跨平台支持** - iOS、Android 和 Web
- 🎨 **作品展示** - 浏览精美的 AI 创意作品
- 🔍 **搜索和筛选** - 按分类、标签搜索作品
- ⬆️ **作品上传** - 上传你的 AI 创意作品
- ❤️ **点赞功能** - 为喜欢的作品点赞
- 📊 **排序功能** - 按时间或热度排序
- 🏷️ **标签系统** - 使用标签组织作品
- 👤 **个人中心** - 管理个人作品和收藏

## 🛠️ 技术栈

- **前端框架**: Expo React Native
- **编程语言**: TypeScript
- **后端服务**: Supabase (PostgreSQL)
- **导航**: React Navigation
- **状态管理**: React Hooks
- **图片选择**: Expo Image Picker

## 📋 前置要求

- Node.js 16+ 和 npm/yarn
- Expo CLI (`npm install -g expo-cli`)
- Supabase 账号 (免费版即可)

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd labx
```

### 2. 安装依赖

```bash
npm install
```

### 3. 设置 Supabase

#### 3.1 创建 Supabase 项目

1. 访问 [Supabase](https://supabase.com) 并创建账号
2. 创建一个新项目
3. 等待数据库初始化完成

#### 3.2 创建数据库表

在 Supabase Dashboard 中:
1. 进入 SQL Editor
2. 复制 `supabase-schema.sql` 文件的内容
3. 粘贴并执行 SQL 命令

#### 3.3 配置环境变量

1. 复制 `.env.example` 文件为 `.env`
   ```bash
   cp .env.example .env
   ```

2. 在 Supabase Dashboard 中找到你的项目凭据:
   - 点击设置 (Settings) → API
   - 复制 `Project URL` 和 `anon public` key

3. 更新 `.env` 文件:
   ```
   EXPO_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
   EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

### 4. 启动应用

```bash
# 启动开发服务器
npm start

# 或者直接启动特定平台
npm run ios     # iOS 模拟器
npm run android # Android 模拟器
npm run web     # Web 浏览器
```

## 📱 应用截图

应用包含以下主要页面:

1. **首页** - 展示所有 AI 创意作品，支持分类筛选和搜索
2. **作品详情** - 查看作品的详细信息、提示词和标签
3. **上传页面** - 上传新的 AI 创意作品
4. **个人中心** - 查看个人信息和统计数据

## 📁 项目结构

```
labx/
├── src/
│   ├── components/        # 可复用组件
│   ├── constants/         # 常量定义（分类、AI模型等）
│   ├── lib/              # 工具函数和配置
│   │   ├── api.ts        # Supabase API 封装
│   │   └── supabase.ts   # Supabase 客户端配置
│   ├── navigation/       # 导航配置
│   ├── screens/          # 页面组件
│   │   ├── HomeScreen.tsx
│   │   ├── DetailScreen.tsx
│   │   ├── UploadScreen.tsx
│   │   └── ProfileScreen.tsx
│   └── types/            # TypeScript 类型定义
│       ├── database.ts   # 数据库类型
│       └── navigation.ts # 导航类型
├── assets/               # 静态资源
├── App.tsx              # 应用入口
├── supabase-schema.sql  # 数据库架构
└── package.json         # 项目依赖
```

## 🗄️ 数据库架构

### ai_creations 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| created_at | TIMESTAMP | 创建时间 |
| title | TEXT | 作品标题 |
| description | TEXT | 作品描述 |
| image_url | TEXT | 图片 URL |
| category | TEXT | 分类 |
| tags | TEXT[] | 标签数组 |
| ai_model | TEXT | 使用的 AI 模型 |
| prompt | TEXT | AI 提示词（可选）|
| likes | INTEGER | 点赞数 |
| author | TEXT | 作者（可选）|

## 🎨 支持的 AI 模型

- Midjourney
- DALL-E
- Stable Diffusion
- Leonardo AI
- Adobe Firefly
- Other

## 📝 使用说明

### 浏览作品

1. 打开应用后默认显示所有作品
2. 使用顶部的搜索栏搜索作品
3. 点击分类标签筛选不同类型的作品
4. 使用"最新"/"最热"切换排序方式
5. 点击作品卡片查看详情

### 上传作品

1. 点击底部"上传"标签
2. 点击图片区域选择图片
3. 填写标题和描述（必填）
4. 选择分类和 AI 模型
5. 可选填写提示词、作者和标签
6. 点击"上传作品"按钮

### 查看详情

1. 在详情页可以查看:
   - 高清大图
   - 完整描述
   - AI 模型信息
   - 提示词（如果有）
   - 标签
2. 点击"点赞"按钮为作品点赞

## 🔧 开发指南

### 添加新功能

1. 创建新的屏幕组件在 `src/screens/`
2. 在 `src/navigation/index.tsx` 中添加路由
3. 如需新的 API 方法，在 `src/lib/api.ts` 中添加

### 自定义样式

应用使用内联样式，主题色为紫色 (`#8B5CF6`)。你可以在各个组件的 `StyleSheet` 中修改样式。

### 环境配置

- 开发环境: `.env`
- 生产环境: 在 Expo 构建时配置环境变量

## 🚀 部署

### Web 部署

```bash
npm run web
# 构建生产版本
npx expo export:web
```

### 移动应用构建

使用 EAS Build:

```bash
# 安装 EAS CLI
npm install -g eas-cli

# 登录 Expo
eas login

# 配置构建
eas build:configure

# 构建 Android
eas build --platform android

# 构建 iOS
eas build --platform ios
```

## 🐛 故障排除

### Supabase 连接失败

- 检查 `.env` 文件中的 URL 和 Key 是否正确
- 确认 Supabase 项目状态正常
- 查看数据库的 RLS 策略是否正确设置

### 图片上传问题

当前版本使用 URL 作为图片存储方式。如需完整的图片上传功能:

1. 在 Supabase 中创建 Storage Bucket
2. 修改 `UploadScreen.tsx` 中的图片上传逻辑
3. 使用 Supabase Storage API 上传图片

### 依赖安装问题

```bash
# 清除缓存重新安装
rm -rf node_modules package-lock.json
npm install
```

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，请创建 Issue。

---

使用 ❤️ 和 Expo + Supabase 构建
